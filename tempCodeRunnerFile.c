#include <stdio.h>

int main()
{
    int size=0;
    int sum=0;

    int i,a[size],average;
    printf("enter the size of array");
    scanf("%d",&size);
    for(i=0;i<size;i++)
    {
    printf("enter the values");
    scanf("%d",&a[size]);
    if(a[size]%2==0)
    sum=sum+a[size];
   
   
   
   
}
printf("sum=%d\n",sum);
average=sum/size;
printf(%d,average);

   
    return 0;
}